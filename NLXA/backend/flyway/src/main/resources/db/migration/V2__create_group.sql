CREATE TABLE `group` (
                         `id` INT NOT NULL,
                         `description` INT NULL,
                         PRIMARY KEY (`id`)
)
    COLLATE='utf8mb4_general_ci'
;
